{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/ops-api/:feed", "destination": "/api/ops/:feed" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
